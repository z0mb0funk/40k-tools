{
  "private": true,
  "description": "Dev tooling only (headless print-sheet testing). The site itself is static and has no runtime npm dependencies.",
  "devDependencies": {
    "pdf-lib": "^1.17.1",
    "puppeteer": "^25.1.0"
  },
  "allowScripts": {
    "puppeteer@25.1.0": true
  }
}
